 # 무리수는 어떻게 계산할까? — √2는 어떻게 계산할까?

---

# 가. TL;DR

**√2 같은 무리수는 "정확한 공식으로 한 번에 계산"하는 게 아니라, "틀린 추측을 계속 더 나은 추측으로 고쳐나가는" 방식으로 계산합니다.** 대충 찍은 값(예: 1.4)에서 시작해서, "이 값의 제곱이 2보다 큰가 작은가"를 보고 값을 조금 조정하는 과정을 반복하면, 추측이 정답에 무서운 속도로 가까워집니다. 이 조정 규칙이 바로 **뉴턴법(Newton's method)**이고, 이 규칙을 쓰면 반복할 때마다 **맞는 자릿수가 거의 두 배씩 늘어납니다.** 컴퓨터는 이 반복을 수십 번만 돌려도 수십만 자리까지 계산해낼 수 있습니다. 이 글은 그 계산을 실제로(이론상이 아니라 진짜로) 수행해서 보여주는 도구와 함께 제공됩니다.

---

# 나. 뉴턴 근사법의 매우 상세한 설명과 역사

## 나-1. 먼저, 왜 √2는 "골치 아픈" 숫자인가

정수(1, 2, 3...)나 분수(1/2, 3/4...)는 계산이 깔끔합니다. 그런데 √2, √5, 황금비 같은 숫자는 **아무리 정확한 분수를 갖다 대도 딱 맞아떨어지지 않는다**는 게 증명되어 있습니다. 이런 숫자를 "무리수(irrational number)"라고 부릅니다.

예를 들어 √2를 소수로 쓰면:

```
1.41421356237309504880168872420969807856967187537694...
```

이 소수는 끝나지도 않고, 특정 패턴이 반복되지도 않습니다. 즉 "정답을 정확히 다 적는다"는 건 애초에 불가능합니다. 우리가 할 수 있는 건 **"원하는 만큼 가까운 근삿값을 구하는 것"** 뿐입니다. 그리고 이 글이 하는 일이 정확히 이겁니다 — "원하는 만큼"을 소수점 아래 50만 자리까지 늘릴 수 있게 해주는 것이죠.

## 나-2. "추측하고 고쳐나가기" — 직관적으로 이해하기

숫자 맞히기 게임을 떠올려보세요. 누군가 1부터 100 사이의 숫자를 생각하고, 여러분이 추측할 때마다 "더 높다" 또는 "더 낮다"라는 힌트만 줍니다. 이 힌트만으로도 여러분은 빠르게 정답에 접근할 수 있습니다.

√2를 구하는 것도 비슷합니다. "어떤 수를 제곱해서 2가 나오는가?"라는 질문에 대해:

1. 일단 아무 값이나 추측합니다. 예: **1.4**
2. 1.4를 제곱해보면 **1.96**. 2보다 조금 작으니, 추측값을 살짝 올려야 합니다.
3. 이번엔 **1.42**. 제곱하면 **2.0164**. 이제 2보다 조금 크니, 살짝 내려야 합니다.
4. 이 과정을 계속 반복하면 값이 점점 2를 제곱해서 나오는 그 수, 즉 √2에 조여듭니다.

문제는 "얼마나 올리고 내려야 하는가"를 어떻게 정하느냐입니다. 그냥 감으로 조절하면 한없이 느립니다. 여기서 **뉴턴법**이 등장합니다 — "얼마나 조정해야 하는가"를 수학적으로 아주 효율적으로 정해주는 규칙입니다.

## 나-3. 뉴턴법을 비유로 이해하기

뉴턴법의 핵심 아이디어는 이렇습니다.

> **"현재 추측값 주변에서, 원래 문제를 잠깐 동안은 '직선'이라고 치고, 그 직선이 정답을 가리키는 곳으로 바로 점프한다."**

곡선을 아주 가까이서 확대해서 보면, 거의 직선처럼 보입니다. 뉴턴법은 이 사실을 이용합니다. 지금 서 있는 위치에서 곡선에 접하는 직선(접선)을 하나 긋고, "이 직선이라면 정답이 어디였을까?"를 계산해서 그 자리로 점프합니다. 실제 곡선은 완벽한 직선이 아니기 때문에 한 번에 정답을 맞히지는 못하지만, **원래 지점보다 훨씬 더 정답에 가까워집니다.** 이 과정을 몇 번만 반복하면 오차가 눈에 띄게 줄어듭니다.

√2를 구하는 경우, 이 규칙을 풀어서 쓰면 다음과 같은 아주 간단한 식이 나옵니다.

```
새로운 추측 = (이전 추측 + 2 ÷ 이전 추측) ÷ 2
```

즉 **"이전 추측"과 "2를 이전 추측으로 나눈 값"의 평균**을 취하는 것뿐입니다. 손으로도 계산기 없이 해볼 수 있습니다.

- 추측: 1.4
- 2 ÷ 1.4 = 1.42857...
- 평균: (1.4 + 1.42857) ÷ 2 = **1.41429**  (벌써 소수점 둘째 자리까지 맞음: 실제 값 1.41421...)
- 다시 한 번: 2 ÷ 1.41429 = 1.41413..., 평균 = **1.41421356...**  (벌써 소수점 여덟째 자리 가까이 맞음!)

단 두 번의 반복만으로 정확도가 폭발적으로 좋아지는 걸 볼 수 있습니다. 이게 바로 "반복할 때마다 맞는 자릿수가 약 두 배씩 늘어난다"는 현상입니다 (다음 절에서 더 자세히 다룹니다).

## 나-4. 왜 하필 "평균을 내는" 방식이 잘 통할까?

직관적으로 이렇게 이해할 수 있습니다. 어떤 추측값 `x`가 √2보다 크다고 해봅시다. 그러면 `2/x`는 반드시 √2보다 **작습니다** (두 수를 곱하면 정확히 2가 되어야 하니까, 하나가 크면 다른 하나는 작아야 함). 즉 `x`와 `2/x`는 항상 **진짜 정답을 사이에 두고 서 있는 두 값**입니다. 이 둘의 한가운데(평균)를 취하면, 항상 실제 정답 근처로 모이게 되는 것이죠. 게다가 이 "한가운데로 모이는 속도"가 그냥 산술적으로 느린 게 아니라, 제곱으로 빨라지는 매우 특별한 속도입니다.

## 나-5. 역사 — 이 방법은 언제, 누가 만들었을까

흥미로운 점은, **이 방법이 뉴턴보다 훨씬 오래 전부터 존재했다**는 것입니다.

**① 고대 바빌로니아 (기원전 약 1800~1600년경)**
현재까지 남아있는 점토판 중 "YBC 7289"라는 유물이 있는데, 여기에 정사각형과 대각선이 그려져 있고 그 옆에 쐐기문자 숫자로 √2에 해당하는 근삿값이 새겨져 있습니다. 이 값은 60진법으로 기록되어 있는데, 십진수로 환산하면 **1.41421296...** 정도로, 실제 값(1.41421356...)과 소수점 다섯째 자리까지 일치하는 놀랍도록 정밀한 값입니다. 학자들은 바빌로니아인들이 지금 우리가 "뉴턴법"이라 부르는 것과 본질적으로 같은 "평균내기" 반복 절차를 이미 쓰고 있었을 것이라 추정합니다. 그래서 이 방법을 **"바빌로니아 방법(Babylonian method)"**이라고도 부릅니다.

**② 고대 그리스 — 헤론(Heron of Alexandria, 서기 1세기경)**
그리스의 수학자·공학자였던 헤론은 자신의 저서에서 제곱근을 구하는 절차를 명시적으로 기록으로 남겼습니다. 그래서 이 반복 절차는 **"헤론의 방법(Heron's method)"**이라는 이름으로도 널리 불립니다. 수학적 내용은 바빌로니아 방법과 동일합니다 — "추측값과 그 값으로 나눈 몫의 평균을 취한다"는 것이죠.

**③ 17세기 — 아이작 뉴턴과 조셉 랩슨**
지금 우리가 "뉴턴법"이라 부르는 이름의 유래는 아이작 뉴턴(Isaac Newton)입니다. 뉴턴은 1669년경 "De analysi per aequationes numero terminorum infinitas"라는 저작을 **저술**했는데, 여기서 다항방정식의 근을 반복적으로 근사하는 절차를 설명했습니다. (이 저작은 1711년에야 출판되었고, 뉴턴의 방법이 널리 공개된 것은 1736년 《유율법과 무한급수》를 통해서였습니다.) 다만 뉴턴의 원래 설명은 지금 우리가 배우는 형태(도함수를 이용한 일반 공식)와는 조금 달랐고, 표기법도 복잡했습니다.

이후 **조셉 랩슨(Joseph Raphson)**이 1690년 저서 《Analysis Aequationum》에서 이 절차를 훨씬 더 간결하고 대수적인 형태로 다시 정리해서 발표했습니다. 오늘날 대학 수학/공학 교재에서 이 방법을 **"뉴턴-랩슨법(Newton-Raphson method)"**이라고 함께 부르는 이유가 여기에 있습니다.

흥미로운 점은, 뉴턴과 랩슨이 다룬 것은 원래 **다항방정식 전반**을 푸는 일반적인 절차였다는 것입니다. "제곱근 구하기"는 이 일반적인 방법을 `f(x) = x² - N`이라는 아주 단순한 방정식에 적용했을 때 나오는 **특수한 경우**에 불과합니다. 즉, 역사적으로는:

```
바빌로니아/헤론의 "제곱근 평균내기" 절차  (특수한 경우, 훨씬 오래됨)
              ↓ (수천 년 후, 일반화)
뉴턴-랩슨의 "일반 방정식 근 구하기" 절차  (일반적인 경우, 17세기)
```

이런 순서로 발전한 셈입니다. 우리가 지금 "뉴턴법으로 제곱근을 구한다"고 말할 때는, 사실 역사적으로 더 오래된 바빌로니아/헤론의 방법을, 더 나중에 정립된 일반적인 뉴턴-랩슨의 틀로 다시 표현하고 있는 것입니다.

**④ 20세기 이후 — 컴퓨터 시대**
20세기 중반 전자식 컴퓨터가 등장하면서, 뉴턴법은 하드웨어 나눗셈 회로, 제곱근 함수(`sqrt`), 나아가 부동소수점 연산 전반을 구현하는 데 실전적으로 매우 중요한 도구가 되었습니다. 반복 횟수가 적고 수렴이 빨라서, 정밀도를 조금씩 늘려가며 계산하는 데 특히 유리했기 때문입니다. 유명한 예로 1990년대 비디오 게임(퀘이크 III)에서 조명 계산을 빠르게 하려고 **비트 트릭(매직 상수 `0x5f3759df`) + 뉴턴법 1회**를 조합한 "고속 역제곱근(fast inverse square root)" 트릭이 쓰인 사례도 있습니다. 오늘날 이 글과 함께 제공되는 도구처럼 브라우저에서 수십만 자리 정밀도를 계산하는 것도, 결국 수천 년 전 점토판에 새겨졌던 것과 같은 원리를 그대로 쓰고 있는 것입니다.

## 나-6. 왜 "자릿수가 두 배씩" 늘어나는가 — 조금 더 감을 잡기

앞의 예시에서 1.4 → 1.41429 → 1.41421356... 순서로 맞는 자릿수가 2자리 → 5자리 → 8자리 이상으로 늘어난 걸 보셨을 겁니다. 이건 우연이 아니라 뉴턴법의 본질적인 성질입니다.

직관적인 이유는 이렇습니다. 오차가 작을 때는(추측값이 정답에 꽤 가까울 때는) 곡선과 접선이 거의 구분되지 않을 정도로 비슷해집니다. 그런데 "곡선과 접선의 차이"는 오차의 **제곱**에 비례해서 작아지는 성질이 있습니다 (곡선을 휘게 만드는 원인인 2차 이상의 항 때문입니다). 그래서 오차가 한 번 줄어들 때, 다음 오차는 그 오차의 제곱만큼 작아집니다.

- 오차가 0.01 (약 2자리 정확)이었다면 → 다음 오차는 약 0.0001 (약 4자리 정확)
- 오차가 0.0001 (4자리)이었다면 → 다음 오차는 약 0.00000001 (8자리)

이렇게 "맞는 자릿수"가 거의 정확히 두 배씩 뛰는 것입니다. 이 성질을 수학 용어로 **2차 수렴(quadratic convergence)**이라 부릅니다. (이 부분의 엄격한 수식 증명은 아래 '다' 문서 4장에 있습니다.)

## 나-7. 그래서 컴퓨터는 뭐가 다른가?

사람이 손으로 계산할 때는 "1.4"라는 15자리 정도의 소수로 만족하지만, 컴퓨터는 이 반복 절차를 정수 연산만으로, 자릿수 제한 없이 수행할 수 있습니다(구현에 쓰이는 `BigInt`가 그 역할을 합니다 — 부록 C 참조). 그리고 반복 횟수는 자릿수에 비례해서 늘어나는 게 아니라 **자릿수의 로그**에 비례해서만 늘어나기 때문에 — 예를 들어 10만 자리를 구하는 데도 이론상 20번이 채 안 되는 반복이면 충분합니다 — 수십만 자리도 순식간에 계산할 수 있는 것입니다. (실제 걸리는 시간에 대한 자세한 분석은 '다' 문서 6장을 참고하세요.)

---

# 다. (이전 버전) 원본 기술 문서

> 아래는 이전에 작성했던 기술 문서 전체를 그대로 보관한 내용입니다. 이 글의 실제 코드는 맨 아래 **부록 A/B/C**에 모아두었습니다. 아래 '다' 문서는 그 코드와 직접 연결되는 상세 설명입니다.

## 1. 이것은 무엇인가

이 글과 함께 제공되는 도구는 √2, √5, ∛7, 황금비(φ) 같은 **무리수를 소수점 아래 원하는 자릿수(최대 500,000자리)까지 실제로 계산**해서 보여주는 웹 도구입니다.

여기서 핵심은 "실제로"라는 단어입니다. 대부분의 계산기는 자바스크립트의 기본 실수 타입(`double`)을 쓰는데, 이 타입은 약 15~16자리 이상은 정확하게 표현하지 못합니다. 이 도구는 그 한계를 피하기 위해 `BigInt`(임의 크기 정수)를 이용한 **고정소수점 산술**과 **뉴턴법(Newton's method)**을 직접 구현해서, 정말로 자릿수만큼 반복 계산을 수행합니다.

부가적으로 두 가지를 더 보여줍니다.
- 반복할 때마다 정답과 몇 자리가 일치하는지 기록하는 표 (수렴 속도를 눈으로 확인)
- 뉴턴법의 기하학적 원리(접선이 x축과 만나는 점으로 다음 근사값을 구하는 과정)를 보여주는 애니메이션 그래프

## 2. 왜 필요한가 — double의 한계

컴퓨터의 기본 실수 타입인 `double`(배정밀도 부동소수점, IEEE 754)은 값을 아래와 같은 형태로 저장합니다.

```
값 = 부호 × 가수부(mantissa) × 2^지수
```

가수부는 52비트인데, 이는 십진수로 환산하면 **약 15~17자리의 유효숫자**에 해당합니다. 그 이상의 자리는 저장할 공간 자체가 없어서 반올림되거나 버려집니다.

예를 들어 자바스크립트 콘솔에서 `Math.sqrt(5)`를 계산하면

```
2.23606797749979
```

딱 여기서 끊깁니다. 16번째 자리부터는 애초에 존재하지 않는 정보입니다. "이론적으로 더 정밀한 값이 있겠지만 표현을 못 한다"가 아니라, 계산 과정 자체가 그 이상의 정밀도를 만들어내지 않습니다.

**이게 왜 문제가 되는가?**
- 암호학, 수치해석 연구, 원주율/무리수 계산 기록 경쟁 등에서는 수백~수만 자리의 정밀도가 필요합니다.
- 여러 번의 연산을 연쇄적으로 수행하면 double의 반올림 오차가 누적되어, 15자리는커녕 그보다 훨씬 적은 자리만 신뢰할 수 있게 되는 경우도 흔합니다.

그래서 이 도구는 double을 아예 계산의 핵심에서 빼고, **정수 연산만으로 실수를 흉내 내는 방식(고정소수점)**을 택했습니다. 정수 연산은 `BigInt`를 쓰면 자릿수 제한이 없기 때문입니다.

## 3. 핵심 수학 1 — 뉴턴법(Newton's Method)

### 3.1 일반 원리

뉴턴법은 방정식 `f(x) = 0`의 해를 찾는 반복 알고리즘입니다. 현재 근사값 `x_n`에서 함수의 접선을 그리고, 그 접선이 x축과 만나는 점을 다음 근사값 `x_{n+1}`로 삼습니다.

```
x_{n+1} = x_n - f(x_n) / f'(x_n)
```

기하학적으로는 "곡선 위의 한 점에서 접선을 그어 x축과 만나는 지점으로 점프하는 것"을 반복하는 과정입니다. 접선은 곡선을 근사하므로, 시작점이 해에 충분히 가깝고 `f'`이 매끄럽게 변하는 함수라면 이 점프는 매번 해에 훨씬 더 가까워지게 됩니다.

### 3.2 이 도구에서 쓰인 구체적인 식

**① N의 k제곱근** (√2, ∛7, 사용자 지정 N과 k 포함)

구하려는 것은 `x^k = N`, 즉 `f(x) = x^k - N = 0`의 해입니다. `f'(x) = k·x^(k-1)`이므로 일반 뉴턴 공식에 대입하면:

```
x_{n+1} = x_n - (x_n^k - N) / (k·x_n^(k-1))
        = ((k-1)·x_n^k + N) / (k·x_n^(k-1))
```

이것이 제곱근 뉴턴 업데이트입니다. k=2일 때는 우리가 흔히 알고 있는 "바빌로니아 제곱근법"과 동일한 식이 됩니다.

```
x_{n+1} = (x_n + N/x_n) / 2   (k=2인 경우로 정리하면 이 형태와 동치)
```

(실제 코드는 **부록 A** 참조.)

**② 황금비 φ**

φ는 `x² = x + 1`, 즉 `f(x) = x² - x - 1 = 0`의 양의 해입니다. `f'(x) = 2x - 1`이므로:

```
x_{n+1} = x_n - (x_n² - x_n - 1) / (2x_n - 1)
        = (x_n² + 1) / (2x_n - 1)
```

(실제 코드는 **부록 B** 참조.)

### 3.3 초기값은 어떻게 정하는가

뉴턴법은 시작점이 해에서 너무 멀면 수렴하지 않거나 엉뚱한 곳으로 발산할 수 있습니다. 다행히 이 도구가 다루는 함수들(거듭제곱, 황금비)은 초기값이 웬만큼만 가까우면 안정적으로 수렴하는 성질을 가지고 있습니다. (단, φ의 업데이트 식 `(x²+1)/(2x-1)`은 `x → 0.5`에서 분모가 0에 가까워져 발산하므로, 초기값이 0.5 근처일 때는 주의가 필요합니다. **부록 B** 참조.)

그래서 이 도구는 영리한 전략을 씁니다.

1. 먼저 double 정밀도로 `Math.pow(N, 1/k)` 같은 저정밀 근사값을 빠르게 구합니다. (이 값은 15자리 정도만 맞지만, "시작점"으로는 충분합니다.)
2. 이 값을 BigInt 고정소수점 형태로 변환해서 뉴턴 반복의 `x_0`로 사용합니다.

즉, "정밀한 답을 처음부터 무한 정밀도로 찾으려는 것"이 아니라 "대충 맞는 곳에서 시작해서, 정수 연산만으로 정밀도를 스스로 불려나가는" 방식입니다.

## 4. 핵심 수학 2 — 왜 자릿수가 반복마다 2배씩 늘어나는가

이 도구의 표(반복 단계별 실제 수렴 기록)를 보면 "일치하는 자릿수"가 반복할 때마다 대략 두 배씩 커지는 것을 확인할 수 있습니다. 이는 우연이 아니라 뉴턴법의 **2차 수렴(quadratic convergence)** 이라는 성질 때문입니다.

### 4.1 증명 스케치

참값을 `r`이라 하고, n번째 근사값의 오차를 `e_n = x_n - r`이라 둡시다. `f`를 `x_n` 근처에서 테일러 전개하면:

```
0 = f(r) ≈ f(x_n) + f'(x_n)(r - x_n) + (1/2)f''(x_n)(r - x_n)²
```

뉴턴 업데이트 식 `x_{n+1} = x_n - f(x_n)/f'(x_n)`을 대입해서 정리하면 다음과 같은 관계가 나옵니다.

```
e_{n+1} ≈ (f''(x_n) / (2·f'(x_n))) × e_n²
```

즉 **다음 오차는 이전 오차의 제곱에 비례**합니다. 오차가 제곱으로 줄어든다는 것은, 오차를 십진수 자릿수로 환산했을 때(오차의 자릿수 ≈ -log₁₀(오차)) **맞는 자릿수가 매 반복마다 대략 2배로 늘어난다**는 뜻입니다.

예를 들어 오차가 10⁻⁵ (5자리 정확)이면 다음 반복에서는 오차가 대략 (10⁻⁵)² = 10⁻¹⁰ (10자리 정확) 수준이 됩니다. 이 도구의 "반복마다 정확한 자릿수가 약 2배로 늘어난다"는 설명 문구는 이 사실을 그대로 반영한 것입니다.

### 4.2 실용적 의미

이 성질 덕분에 자릿수를 늘리는 데 필요한 반복 횟수는 자릿수에 비례하지 않고 **자릿수의 로그(log)에 비례**합니다. 100,000자리가 필요해도 log₂(100,000) ≈ 17번 정도의 반복이면 이론상 충분합니다. 구현의 반복 상한(200회)이 넉넉한 이유가 여기에 있습니다. (**부록 C** 참조.)

## 5. 구현 원리 — BigInt로 실수 흉내 내기 (고정소수점)

`BigInt`는 정수만 다룹니다. 소수를 표현하려면 트릭이 필요한데, 이 도구는 **고정소수점(fixed-point)** 방식을 씁니다.

원리는 간단합니다. 실수 `r`을 저장하고 싶으면, `r`에 10의 거듭제곱을 곱해서 정수로 만든 뒤 그 정수만 저장합니다.

```
R = round(r × 10^P)
```

여기서 `P`는 "소수점 아래 몇 자리까지 정수로 취급할 것인가"를 정하는 값입니다. 예를 들어 `r = 2.236`이고 `P = 3`이면 `R = 2236`이라는 정수로 저장하는 식입니다. 이렇게 하면 덧셈·뺄셈은 그대로 정수 덧셈·뺄셈이 되고, 곱셈은 정수 곱셈 후 `10^P`로 나누는 것으로, 나눗셈은 분자에 `10^P`를 미리 곱해준 뒤 정수 나눗셈을 하는 것으로 흉내 낼 수 있습니다. 위 3.2와 **부록 A/B**의 코드에서 `* scale`, `/ scale`이 반복적으로 등장하는 이유가 이것입니다.

### 5.1 GUARD(여유 자릿수)는 왜 있는가

정수 나눗셈은 나머지를 버리기 때문에 매 연산마다 아주 작은 오차(최대 1 ULP 수준)가 생깁니다. 이 오차가 반복을 거듭하며 누적되면, 목표한 자릿수의 맨 끝부분이 틀어질 수 있습니다.

이를 막기 위해 실제 목표 자릿수보다 30자리 더 여유 있게 계산한 뒤, 최종적으로 결과를 자를 때 이 여유분을 버립니다. 즉 "목표 자릿수 + 30자리"만큼 내부적으로 계산하고, 겉으로는 목표 자릿수만 보여주는 것입니다. 이 30자리 여유분을 **부록 C**에서는 `GUARD = 30`이라는 상수로 두고 있습니다. 이런 여유 자릿수를 얼마나 둬야 안전한지는 실제로 임의정밀도 수치 계산 분야에서도 이론적으로 분석되는 주제입니다(오차가 1 ULP 이내임을 보장하기 위한 최소 가드 자릿수를 유도하는 연구들이 있습니다).

## 6. 성능 특성 — 왜 자릿수가 늘면 급격히 느려지는가

이 도구는 "100,000자리도 1초 내외"라고 안내하고 있습니다. 이 주장을 좀 더 깊이 들여다보면:

- BigInt의 곱셈·나눗셈은 (특히 간단한 구현에서는) 자릿수 `n`에 대해 **O(n²)** 의 시간이 걸리는 방식(스쿨북 알고리즘)을 씁니다. 즉 자릿수를 2배로 늘리면 한 번의 곱셈/나눗셈에 걸리는 시간은 4배가 됩니다.
- 나눗셈은 곱셈보다도 일반적으로 더 무거운 연산으로 알려져 있습니다.
- 뉴턴법 자체의 반복 횟수는 log(자릿수)에 비례해 매우 적지만(4장 참고), **각 반복 안에서 수행하는 곱셈·나눗셈 하나하나의 비용**이 자릿수의 제곱에 비례해서 늘어납니다.

따라서 전체적으로 총 계산 시간은 대략 **자릿수의 제곱에 비례**해서 늘어난다고 보는 것이 이론적으로 안전합니다. 100,000자리에서 1초 정도였다면, 이 도구가 허용하는 최댓값인 500,000자리(5배)에서는 산술적으로 약 25배(20여 초 이상) 걸릴 가능성이 있습니다. "자릿수를 늘려도 수십 ms~1초 내외"라는 안내는 중간 자릿수 구간에서는 맞을 수 있지만, 최댓값 근처에서는 체감 속도가 그보다 느려질 수 있다는 점을 감안하는 것이 좋습니다.

---

## 7. 요약 — 무엇이 어떤 수학과 연결되는가

```
[도구의 요소]                          → [연결된 수학/개념]                     (코드 위치)

· double 대신 BigInt를 쓰는 이유
    → IEEE 754 부동소수점의 유효숫자 한계(약 15~16자리)              (부록 C)

· 반복해서 근사값을 개선하는 로직
    → 뉴턴법 (x_{n+1} = x_n − f(x_n)/f'(x_n))                     (부록 A·B)

· √N, ∛N, N의 k제곱근 계산식
    → f(x) = x^k − N 에 뉴턴법을 적용한 결과                        (부록 A)

· 황금비 계산식
    → f(x) = x² − x − 1 에 뉴턴법을 적용한 결과                     (부록 B)

· "일치 자릿수가 반복마다 2배"
    → 뉴턴법의 2차 수렴(오차가 제곱으로 감소)                        (본문 4장)

· BigInt로 소수를 표현하는 방법
    → 고정소수점 표현 (R = round(r × 10^P))                        (부록 C)

· GUARD = 30
    → 반복 나눗셈의 반올림 오차를 흡수하는 여유 자릿수               (부록 C)

· double로 초기값을 잡는 이유
    → 초기값이 해에 가까울수록 안정적·빠르게 수렴                    (부록 C)

· 자릿수를 늘릴수록 느려지는 이유
    → BigInt 곱셈/나눗셈의 O(n²) 시간복잡도                         (본문 6장)
```

 
---

# 부록: 실제 구현 코드

아래 코드는 위 본문의 설명과 1:1로 대응하는 실제 구현입니다. 각 부록의 첫 줄에 "본문 몇 절의 코드판인지"를 표시해두었으니, 본문을 다시 보지 않아도 이 부록만으로 이해할 수 있습니다.

## 부록 A: 제곱근 뉴턴 업데이트 (`nthRootStep`)

> 본문 3.2 ①의 코드판입니다.

**수식**: `x_{n+1} = ((k-1)·x_n^k + N) / (k·x_n^(k-1))`

**전체 코드**:

```js
// k제곱근 뉴턴 업데이트: x_{n+1} = ((k-1)x^k + N) / (k * x^(k-1))
// X, Nfp_asInt, scale 은 모두 고정소수점 정수 (실수 × 10^P)
function nthRootStep(X, k, Nfp_asInt, scale) {
  // x^(k-1), x^k 를 고정소수점으로 계산
  let pk1 = X; // x^1
  for (let i = 2; i <= k - 1; i++) pk1 = (pk1 * X) / scale;
  const pk = (pk1 * X) / scale; // x^k

  const numerator = BigInt(k - 1) * pk + Nfp_asInt; // (k-1)x^k + N  (고정소수점)
  const denom = BigInt(k) * pk1;                    // k * x^(k-1)  (고정소수점)
  return (numerator * scale) / denom;
}
```

**핵심 3줄** (수식과 1:1 대응):

```js
const numerator = BigInt(k - 1) * pk + Nfp_asInt; // (k-1)·x^k + N
const denom     = BigInt(k)   * pk1;              // k·x^(k-1)
return (numerator * scale) / denom;
```

**k=2일 때**: 이 식은 `(x + N/x) / 2` 로 줄어듭니다 — 즉 "이전 추측과 N을 이전 추측으로 나눈 값의 평균"이라는, '나-3'에서 본 그 평균내기와 동일합니다.

## 부록 B: 황금비 뉴턴 업데이트 (`phiStep`)

> 본문 3.2 ②의 코드판입니다.

**수식**: `x_{n+1} = (x_n² + 1) / (2x_n - 1)`

**전체 코드**:

```js
// 황금비 뉴턴 업데이트: x_{n+1} = (x^2+1) / (2x-1)
// f(x) = x^2 - x - 1 의 양의 근을 구한다
function phiStep(X, scale) {
  const x2 = (X * X) / scale;
  const numerator = x2 + scale;      // x^2 + 1
  const denom = 2n * X - scale;      // 2x - 1
  return (numerator * scale) / denom;
}
```

**주의**: 이 식은 `x → 0.5` 에서 분모가 0에 가까워져 발산합니다. 초기값이 0.5 근처일 때는 반복이 불안정해질 수 있으므로, 초기값은 충분히 1보다 큰 값(예: double 근사값)에서 시작합니다.

## 부록 C: 고정소수점과 계산 루프

> 본문 5장, 5.1, 6장의 코드판입니다.

**고정소수점 표현**: 실수 `r` 을 정수 `R = round(r × 10^P)` 로 저장합니다.

```js
const GUARD = 30; // 계산 중 오차 누적 방지용 여유 자릿수
const P = digits + GUARD;
const scale = 10n ** BigInt(P);
```

**초기값**: double 정밀도로 대충 구한 근사값을 고정소수점 정수로 변환합니다.

```js
function initialGuess(approxDouble, P) {
  const initDigits = 15;
  const initScale = 10n ** BigInt(initDigits);
  let X = BigInt(Math.round(approxDouble * Number(initScale)));
  if (P > initDigits) {
    X = X * (10n ** BigInt(P - initDigits));
  } else if (P < initDigits) {
    X = X / (10n ** BigInt(initDigits - P));
  }
  return X;
}
```

**계산 루프**: 뉴턴법을 반복 상한까지 돌립니다. 반복 상한이 200인 이유는, 2차 수렴이면 50만 자리도 이론상 20회면 충분하지만, 안전 상한을 넉넉히 잡아둔 것입니다.

```js
while (iter < 200) {
  // ... (한 반복)
  let next = (target.kind === 'phi')
    ? phiStep(X, scale)
    : nthRootStep(X, target.k, NfpAsInt, scale);
  // ... (수렴 판정)
}
```

**결과 문자열 변환**: 저장된 정수 `X` 를 다시 소수 문자열로 바꿉니다.

```js
function toDigitString(X, P) {
  const scale = 10n ** BigInt(P);
  const neg = X < 0n;
  const Xa = neg ? -X : X;
  const intPart = Xa / scale;
  const fracPart = Xa % scale;
  return (neg ? '-' : '') + intPart.toString() + '.' + fracPart.toString().padStart(P, '0');
}
```

---

 

---
---

# 라. 더 알아보기 — √2를 구하는 다른 방법들

지금까지 이 글은 **뉴턴법**을 중심으로 √2를 계산하는 방법을 설명했습니다. 그런데 뉴턴법이 **√2를 구하는 유일한 방법은 아닙니다.** 오히려 물어야 할 것은 **"왜 뉴턴법이 가장 널리 쓰이는가"** 입니다.

이 섹션은 **"뉴턴법 = 유일"이라는 오해를 막기 위해** 마련했습니다. √2를 구하는 10가지 방법을 비교하고, 각각의 수렴 속도·장단점·용도를 정리합니다. 마지막에는 **π, e 같은 초월수**에는 왜 뉴턴법이 통하지 않는지도 다룹니다.

## 라-1. 결론 먼저

**아니다.** 뉴턴법은 **가장 유명하고 효율적인 방법 중 하나**일 뿐, **유일하지 않다.** √2를 구하는 방법은 **수십 가지**가 있고, 각각 **수렴 속도·구현 난이도·역사적 배경**이 다르다.

즉, "√2를 구하는 유일한 방법"은 존재하지 않는다. 오히려 물어야 할 것은 **"왜 뉴턴법이 가장 유명한가"** 다.

## 라-2. 뉴턴법이 "유일"처럼 보이는 이유

### (1) 수렴 속도가 압도적
매 반복마다 **자릿수가 2배**로 늘어난다(2차 수렴). 100자리 → 200자리 → 400자리 → … 즉 **약 10회 반복이면 1000자리** 이상이다.

### (2) 구현이 놀랍도록 단순
```
x ← (x + N/x) / 2
```
**덧셈·나눗셈·평균**뿐이다. 이것보다 단순한 방법은 거의 없다.

### (3) 역사가 압도적으로 길다
**기원전 1800~1600년경** 바빌로니아 점토판 YBC 7289까지 거슬러 올라간다. 즉 **약 3800년간** 쓰인 방법이다.

### (4) 컴퓨터 과학의 널리 쓰이는 표준
하드웨어 `sqrt`·`div` 회로, Quake III의 fast inverse sqrt, 임의정밀도 라이브러리(GMP, MPFR) 등에서 뉴턴법 계열이 널리 쓰인다.

이 4가지가 겹쳐서 **"뉴턴법 = √2 계산의 표준"** 이라는 인식이 생겼다. 하지만 **"표준"이지 "유일"은 아니다.**

## 라-3. 다른 방법들 — 구체적으로

### (1) 이분법 (Bisection) — 가장 원시적

**아이디어**: `f(x) = x² - 2`의 부호가 바뀌는 구간을 반씩 좁힌다.

```
[a, b]에서 f(a) < 0 < f(b)이면:
  m = (a + b) / 2
  f(m) < 0이면 a ← m
  f(m) > 0이면 b ← m
```

- **수렴 속도**: 매 반복마다 **1비트** (자릿수로는 0.3자리)
- **장점**: **무조건 수렴** (f가 연속이면)
- **단점**: **매우 느림** — 1000자리 구하려면 **약 3300회** 반복
- **용도**: 뉴턴법의 **초기값을 안전하게 잡는 용도**로 자주 쓰임

√2를 이분법으로 구하면 1.4와 1.5 사이 → 1.45 → 1.425 → 1.4125 → … 로 좁혀진다. 1000자리에는 약 3300회 반복이 필요하다(뉴턴법은 약 10회).

### (2) 할선법 (Secant Method) — 뉴턴법의 사촌

**아이디어**: 뉴턴법은 **도함수 f'(x)** 를 쓰는데, 할선법은 **두 점을 잇는 직선**으로 근사한다.

```
x_{n+1} = x_n - f(x_n) · (x_n - x_{n-1}) / (f(x_n) - f(x_{n-1}))
```

- **수렴 속도**: **1.618차** (황금비! — 할선법의 수렴 차수가 방정식 `x² = x + 1`의 근이기 때문)
- **장점**: **도함수를 몰라도 됨**
- **단점**: 뉴턴법보다 **약간 느림**, 초기값 2개 필요
- **용도**: f'을 구하기 어려운 경우

√2의 경우 `f(x) = x² - 2`, `f'(x) = 2x`라 뉴턴법이 쉬우므로 실전에선 잘 안 쓴다.

### (3) 고정점 반복 (Fixed-point Iteration)

**아이디어**: `x = g(x)` 꼴로 바꿔서 반복한다.

√2의 경우:
```
x = 2/x        (g(x) = 2/x)     → 수렴 안 함 (진동)
x = (x + 2/x)/2                 → 이게 뉴턴법!
x = 1 + 1/(1+x)                 → 연분수 (1차 수렴)
```

- **수렴 속도**: **1차** (매 반복마다 일정 비율로 오차 감소)
- **장점**: 단순
- **단점**: **g'(r)의 크기에 따라 수렴 여부 결정**

√2의 연분수 표현 `√2 = 1 + 1/(2 + 1/(2 + 1/(2 + ...)))` 은 고정점 반복의 일종이며 **1차 수렴**이다(뉴턴법보다 훨씬 느림).

### (4) 연분수 (Continued Fraction) — 이론적으로 아름다움

√2의 연분수는 `√2 = [1; 2, 2, 2, 2, ...]` 다.

- **수렴 속도**: 1차 (매 단계마다 약 1자리)
- **장점**: **패턴이 명확** — √2는 순환 연분수
- **단점**: **느림**

수학적으로 아름답지만 실전 계산에는 부적합하다.

### (5) 테일러 급수 (일반화 이항급수)

```
√2 = (1 + 1)^(1/2) = 1 + 1/2 - 1/8 + 1/16 - 5/128 + ...
```

- **수렴 속도**: 1차 (매우 느림)
- **문제**: **수렴 반경이 1**이라 √2는 **경계**에 걸린다 → 수렴은 하지만 **매우 느림**

√2보다 π, e 계산에 유리하다. √2에는 비효율적이다.

### (6) CORDIC — 임베디드/FPGA의 고전적 표준

**아이디어**: 회전 행렬을 이용해 벡터를 회전시키며 `√(x² + y²)` 를 계산한다.

- **수렴 속도**: 매 반복마다 **1비트**
- **장점**: **덧셈/시프트만**으로 구현 (곱셈 불필요)
- **단점**: 느림
- **용도**: **임베디드, FPGA, 오래된 계산기** (현대 CPU는 CORDIC 대신 다항식 근사 + 각도 감소를 주로 사용)

### (7) 골드슈미트 (Goldschmidt) — GPU의 표준

**아이디어**: 뉴턴법과 유사하지만 **곱셈만** 사용한다.

```
y_0 ≈ 1/√N
b_0 = N
x_0 = N
반복:
  y_{n+1} = y_n (3 - b_n y_n²) / 2
  b_{n+1} = b_n y_{n+1}²
  x_{n+1} = x_n y_{n+1}
```

- **수렴 속도**: **2차** (뉴턴법과 동일)
- **장점**: **곱셈만** 사용 (나눗셈 불필요) → **GPU/하드웨어에 유리**
- **단점**: 수치 안정성 뉴턴법보다 약간 나쁨

현대 GPU의 `rsqrt`(역제곱근)에 사용된다.

### (8) 아날로그/기하 작도 — 고대 방식

고대 그리스에서는 정사각형 대각선 = √2 × 변임을 이용해 기하학적으로 √2를 작도했다. 뉴턴 이전에는 이게 표준이었다.

- **정밀도**: **한계** (자와 컴퍼스)
- **현대**: 교육용

### (9) 뉴턴법의 변형들

- **하우스홀더법 (Householder)**: **3차 수렴** (자릿수 3배) — 더 빠르지만 복잡
- **할리의 방법 (Halley)**: **3차 수렴** — 유리함수 근에 유리
- **오일러-체비셰프**: 다항식 근사

√2는 2차 수렴이면 충분하므로 잘 안 쓴다.

### (10) 수치적분/미분방정식

`dx/dt = x, x(0) = 1` 을 풀어 `e` 를 구하는 식으로, √2도 **미분방정식**으로 접근 가능하다. 하지만 과하다.

## 라-4. 그럼 "왜 뉴턴법이 표준인가"

√2의 경우를 보자.

- `f'(x) = 2x` 가 **자명** → 뉴턴법의 유일한 단점(도함수 필요)이 사라진다.
- 초기값도 **1.4 정도면 충분**하다.
- 결과적으로 **뉴턴법이 압도적으로 유리**하다.

수렴 속도만 봐도, 1000자리를 얻는 데 뉴턴법은 약 10회, 이분법은 약 3300회, 할선법은 약 20회, 연분수는 약 1000회다.

**그래서 "√2에는 뉴턴법이 최선"** 이지, **"유일"** 은 아니다.

## 라-5. "유일하지 않다"의 좋은 예 — π와 e

- **π**: 아르키메데스(다각형), 마친(급수), 라마누잔(초고속), Chudnovsky(현재 표준)
- **e**: `(1 + 1/n)^n`, 테일러 급수, 연분수

**π, e는 뉴턴법이 통하지 않는다.** 방정식의 근이 아니라 상수이기 때문이다. 즉, **"무리수 = 뉴턴법"이라는 공식은 √2 같은 대수적 무리수에만 국한**된다.

**초월수(π, e)** 는 **급수/연분수/특수 알고리즘**이 필요하다.

## 라-6. 비유로 정리

"서울에서 부산 가는 방법은 KTX가 유일한가?" → **아니다** (비행기, 버스, 자가용, 자전거…)

"그럼 KTX가 최선인가?" → **대부분의 경우 그렇다** (빠르고, 편하고, 합리적)

√2에 뉴턴법도 마찬가지다.

- **유일하지 않음** (이분법, 연분수, 할선법, CORDIC…)
- **하지만 최선** (2차 수렴 + 단순 구현 + 자명한 도함수)
- **역사적으로도 표준** (약 3800년)

## 라-7. 이 글(본문)이 "뉴턴법"을 주인공으로 한 이유

앞의 '나'·'다' 섹션이 뉴턴법을 중심으로 한 건 **"유일해서"가 아니라**:

1. **√2, √5, φ 등 대수적 무리수에 최적**
2. **구현이 단순** (교육용으로 완벽)
3. **역사가 길고 흥미로움** (바빌로니아 → 헤론 → 뉴턴)
4. **2차 수렴이 극적으로 보임** (자릿수 2배)

**즉, "교육적 목적"에 뉴턴법이 최적**이기 때문이다.

만약 이 글이 **π를 다뤘다면** 마친 급수, 라마누잔 급수, Chudnovsky 알고리즘을 다뤘을 것이다. **즉, 무리수의 종류에 따라 최선의 방법이 다르다.**

## 라-8. 결론

**"√2 = 뉴턴법"은 "√2 = 뉴턴법이 최선"의 줄임말**이지, **"유일"이 아니다.** 이 구분이 이 글의 완성도를 한 단계 더 올려줄 수 있다.